#include<stdio.h>
int main()
{
   /* int a;
    printf("enter the number:");
    scanf("%d",&a);
    printf("%d",a);
    return 0; */

              // interest program
   /* float p,r,t,SI;
    printf("enter the number: ");
    scanf("%f%f%f",&p,&r,&t);
    SI=(p*r*t)/100;
    printf("simple interest:%f ",SI); 
    return 0;*/


          // Quotient and Remainder
   /* int divident,divisor,Quotient, Remainder;
    printf("enter the nuber of divident:");
    scanf("%d",&divident);
    printf("entre the number of divisor:");
    scanf("%d",divisor);
    Quotient=divident/divisor;
    Remainder=divident%divisor;
    printf("Quotient %d\n",Quotient);
    printf("Remeinder%d",Remainder);
    return 0; */

    // even and odd
    /*int a;
    printf("enter the number : ");
    scanf("%d",&a);
    if(a%2==0)
    {
        printf("the number is even");
    }
    else{
        printf("the number is odd");
    }
    return 0;*/

     

}